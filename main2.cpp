#include <bits/stdc++.h>
using namespace std;
int main()
{
  ios::sync_with_stdio(0);
  cin.tie(0);
  int n, h;
  cin >> n >> h;
  vector<string> vvi(n);
  for (auto &a : vvi)
    cin>>a;
  vector<int> received(n
  );
  while(--h)
  {

  }
  
}