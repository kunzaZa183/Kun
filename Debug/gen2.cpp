#include <bits/stdc++.h>
using namespace std;
int main()
{
  cin.tie(0)->sync_with_stdio(0);
  cin.exceptions(cin.failbit);
  freopen("input.txt", "w", stdout);
  srand(time(NULL));
  const int n = 10, maxtype = 5;
  cout << n << '\n';
  vector<int> vi;
  for (int i = 0; i < n; i++)
    vi.push_back(i / 2 + 1);
  shuffle() 
}