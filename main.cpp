#include <bits/stdc++.h>
using namespace std;
const int maxn = 100000,color = 5;
int arr[maxn];
int segtree[color][4*maxn];
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n,m;
    cin>>n>>m;
    for(int i=0;i<n;i++)
        cin>>arr[i];
     
}