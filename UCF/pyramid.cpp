#include <bits/stdc++.h>
using namespace std;
int main()
{
  ios::sync_with_stdio(0);
  cin.tie(0);
  int tests;
  cin>>tests;
  while(tests--)
  {
    int n;
    cin>>n;
    vector<int> vi(n);
    for(auto &a:vi)
      cin>>a;
    vector<vector<int>> dp(n,vector<int>(n,-1));
    
  }
}